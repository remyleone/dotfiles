#!/usr/bin/env bash
# ----------------------------------------------------------------------
# Description
#   Create a backup copy of the file with a timestamp.
#
# Arguments
#   1 (required) the file to backup.
# ----------------------------------------------------------------------

cp $1 $1_`date +%H:%M:%S_%d-%m-%Y`
