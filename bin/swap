#!/usr/bin/env bash
# ----------------------------------------------------------------------
# Description
#   Swap 2 filenames.
#
# Arguments
#   1 (required) the first file.
#   2 (required) the second file.
# ----------------------------------------------------------------------

local TMPFILE=tmp.$$
mv "$1" $TMPFILE
mv "$2" "$1"
mv $TMPFILE "$2"
